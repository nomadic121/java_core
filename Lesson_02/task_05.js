// 5. Завести три переменных start, end, point. Вывести входит ли point в диапазон от start до end

'use strict'

let start = 0;
let end = 10;
let point = 1;

if (point >= start && point <= end) console.log("point входит в диапазон")
else console.log("point не входит в диапазон")
